# apache-jira-scraper-llm
⚙️ A fault-tolerant pipeline to scrape Apache Jira issues, handle rate limits & failures, and transform data into clean JSONL format for LLM training. Includes checkpointing, retry logic, and structured dataset generation for issue summarization, classification, and Q&A tasks.
